<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title><%= title.split(':')[0] || "404 Not Found" %> </title>
   <style>
      body {
         background-color: #1C0C41;
         color: #FFB423;
         font-family: Arial, sans-serif;
         text-align: center;
         padding: 50px;
      }

      h1 {
         font-size: 3rem;
         margin-bottom: 20px;
      }

      p {
         font-size: 1.2rem;
      }
   </style>
</head>

<body>
   <h1><%= (statusCode || 404) + " " + (error.split(':')[0] || "Not Found") %></h1>
   <p><%= error || "Oops! The page you're looking for doesn't exist."  %> </p>
   <p>Go back to <a href="/">homepage</a>.</p>
</body>

</html>
